@model SkillBridges.ViewModels.LoginViewModel

@{
    ViewData["Title"] = "Login - SkillBridge";
}

<!-- External CSS Link -->
<link rel="stylesheet" href="~/css/site.css" />

<div class="container vh-100 d-flex justify-content-center align-items-center">
    <div class="col-md-5">
        <div class="card shadow-lg border-0 rounded-4">

            <!-- Header -->
            <div class="card-header text-center text-white">
                <h2 class="fw-bold mb-1">SkillBridge</h2>
                <p class="mb-0">Connecting Clients & Professionals</p>
            </div>

            <!-- Body -->
            <div class="card-body p-4">
                <h4 class="text-center fw-semibold mb-4">Welcome Back 👋</h4>
                <form asp-action="Login" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                    <!-- Email -->
                    <div class="mb-3">
                        <label asp-for="Email" class="form-label fw-semibold"></label>
                        <div class="input-group input-group-lg">
                            <span class="input-group-text bg-light">
                                <i class="bi bi-envelope-fill text-primary"></i>
                            </span>
                            <input asp-for="Email" class="form-control" placeholder="Enter your email" />
                        </div>
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>

                    <!-- Password -->
                    <div class="mb-3">
                        <label asp-for="Password" class="form-label fw-semibold"></label>
                        <div class="input-group input-group-lg">
                            <span class="input-group-text bg-light">
                                <i class="bi bi-lock-fill text-primary"></i>
                            </span>
                            <input asp-for="Password" type="password" class="form-control" placeholder="Enter your password" />
                        </div>
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <!-- Forgot Password -->
                    <div class="text-end mb-3">
                        <a asp-action="ForgotPassword" class="text-decoration-none text-primary fw-semibold">
                            Forgot Password?
                        </a>
                    </div>

                    <!-- Login Button -->
                    <div class="d-grid mb-3">
                        <button type="submit" class="btn btn-primary btn-lg shadow-sm">
                            <i class="bi bi-box-arrow-in-right me-2"></i> Login
                        </button>
                    </div>

   


                    <!-- Register -->
                    <div class="text-center mb-2">
                        <span class="text-muted">Don't have an account?</span>
                    </div>

                    <div class="d-grid">
                        <a asp-action="Create" class="btn btn-outline-primary btn-lg fw-semibold">
                            <i class="bi bi-person-plus-fill me-2"></i> Register Now
                        </a>
                    </div>
                </form>

                @if (ViewBag.Message != null)
                {
                <div class="alert alert-success mt-3">
                    @ViewBag.Message
                </div>
                }
            </div>

            <!-- Footer -->
            <div class="card-footer text-center small text-muted">
                &copy; @DateTime.Now.Year SkillBridge. All rights reserved.
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
